---
sidebar_position: 10
---

import { TagDocRender } from '@site/src/components/TagDocRender';
import { AttributeType } from '@site/src/components/AttributeType.class';

# Score

## Overview

Substitutes the Scoreboard-value of a given objective for a provided score-holder; this tag
acts as a placeholder and must thereby be self-closing (does not accept content).

<TagDocRender tag={{
  name: 'score',
  aliases: [],
  selfClosing: true,
  attributes: [
    {
      name: 'name',
      aliases: [],
      description: 'Name of the score-holder (player-name)',
      fallback: null,
      multiValue: false,
      type: AttributeType.STRING,
    },
    {
      name: 'objective',
      aliases: [],
      description: 'Name of the target-objective',
      fallback: null,
      multiValue: false,
      type: AttributeType.STRING,
    },
    {
      name: 'type',
      aliases: [],
      description: [
        'Additional objective-parameter if required',
        'e.g. sand for objective mine_block'
      ],
      fallback: 'null',
      multiValue: false,
      type: AttributeType.STRING,
    },
    {
      name: 'override',
      aliases: [],
      description: 'Value to use as an override',
      fallback: 'null',
      multiValue: false,
      type: AttributeType.STRING,
    },
    {
      name: 'fallback',
      aliases: [],
      description: 'Value to use as a fallback',
      fallback: 'null',
      multiValue: false,
      type: AttributeType.STRING,
    },
    {
      name: 'renderer',
      aliases: [],
      description: [
        'Custom renderer to display a value',
        'Introduces the {score_value} placeholder'
      ],
      fallback: 'null',
      multiValue: false,
      type: AttributeType.MARKUP,
    },
  ]
}} />

## Examples

### Plain Value

In order to retrieve a score-value as-is, there's no need to specify a custom renderer.

:::info
By default, the scoreboard does not track any of the existing vanilla criteria, thus they need to be added manually by invoking: `/scoreboard objectives add health health`. For this web-renderer, we randomly generate them.
:::

```!component-markup
The score-value is: <&b><score name="BlvckBytes" objective="health"/>
```

### Modified Value

With the renderer, score-values may be modified as desired; let's transform the count of half-hearts that `health` shows a fractional number.

```!component-markup
The score-value is: <&b><score
  name="BlvckBytes"
  objective="health"
  renderer={{score_value / 2.0}}
/>
```

### Typed Value

Some objectives require a type, like when one wants to resolve how many blocks of a specific material have been mined by a player.

```!component-markup
The score-value is: <&b><score
  name="BlvckBytes"
  objective="mine_block"
  type="iron_ore"
/>
```